# 多 Agent 投研系统（LangGraph）开发计划

> 目标：在已有「穿透叙事个股分析师」单 Agent 能力之上，用 LangGraph 搭建一个模拟真实投资公司投研流程的多 Agent 系统。
> 五大角色：宏观研究员、行业研究员、投资经理、风控、交易员。
> 原则：**先出可跑的初步版本，再逐阶段完善到终版**；全程贯彻 **Harness Engineering**；支持**动态知识图谱 + GraphRAG**。

---

## 0. 已有资产盘点（起点）

| 资产 | 内容 | 在系统中的定位 |
|------|------|----------------|
| `prompt.txt` | 穿透叙事六步工作流 + 数据质量红线（金额交叉验证、DCF 反算、敏感性校验、异常告警） | 个股深度分析的「方法论红线」，封装进个股分析工具 |
| `penetrate-narrative-stock-analysis` skill | `SKILL.md` + 5 个 `references/` + `scripts/dcf_implied.py` | 「个股分析师」单 Agent 的现成能力（输入公司名 → HTML 简版 + DOCX 详版） |
| 示例报告（贵州茅台 docx） | 完整六步结构 + 执行摘要 + 附录 | 个股分析产出物的**格式范本** |

> 关键结论：你**已经有一个能用的「分析师」**。`行业研究员`（或投资经理调用的「个股深度分析」子 Agent）直接复用这个 skill；系统要做的是把多个角色编排成闭环，并加上知识图谱、风控循环与工程化脚手架。

---

## 1. 总体架构

系统分五层，自上而下是**编排 → 角色 → 记忆/检索 → 数据 → 工程**：

```
┌─────────────────────────────────────────────────────────────┐
│                  LangGraph 编排层 (StateGraph)                 │
│   节点=角色  ·  边/条件边=流转  ·  风控审批循环  ·  HITL 检查点   │
├─────────────────────────────────────────────────────────────┤
│   角色层（5 个 Agent，每个 = LLM + 专属 Prompt + 工具 + 产出 Schema）│
│   宏观研究员 │ 行业研究员 │ 投资经理 │ 风控 │ 交易员            │
├─────────────────────────────────────────────────────────────┤
│        记忆/检索层：动态知识图谱 + GraphRAG（共享上下文）           │
├─────────────────────────────────────────────────────────────┤
│   数据层：行情 / 基本面 / 新闻·事件·公告 / 宏观 / 研报·调研纪要      │
├─────────────────────────────────────────────────────────────┤
│   工程层（Harness）：状态校验 / 日志追踪 / 评估 / 测试 / 配置       │
└─────────────────────────────────────────────────────────────┘
```

- **共享状态（State）**：所有角色通过一份 TypedDict/Pydantic 状态对象交换数据（研究报告、策略、风险指标、订单、成交回报、KG 上下文）。
- **知识图谱 + GraphRAG** 对所有角色可见，作为「机构记忆」：宏观研究员读宏观指标链，行业研究员读产业链上下游，投资经理读「公司—行业—事件」上下文，风控读持仓与敞口关联。
- **数据层** 通过已连接的 `tdx-connector`（通达信，已连接）和可用 skill（`westock-data` / `neodata-financial-search` / `futuapi`）接入。

---

## 2. 五个角色设计

| 角色 | 输入 | 输出（产出 Schema） | 主要工具 | 复用/特色 |
|------|------|---------------------|----------|-----------|
| **宏观研究员** | 当前日期、关注指数（大盘） | 宏观研判报告（趋势/风险/流动性） | 宏观数据、指数行情、新闻 | 大盘走势研判；输出「市场风险偏好」与「仓位上限建议」 |
| **行业研究员** | 标的池、行业 | 行业+个股分析报告 | 行情、基本面、**穿透叙事 skill（dcf_implied.py）** | 个股深度分析直接调用现有 skill；输出「标的评级 + 隐含天花板 L + 买卖点」 |
| **投资经理** | 宏观报告 + 行业报告 + KG 上下文 | 投资决策 + 下单策略（标的/方向/数量/止损止盈） | 汇总、估值定位、技术面 | 综合考虑宏观+行业+基本面+技术面；设定止损止盈；判断是否「值得下单」 |
| **风控** | 下单策略 + 持仓 + 风险限额 | 审批结论（通过/驳回）+ 风险指标（VaR/回撤/集中度/敞口） | 风险计算、限额校验 | 条件边：通过→交易员；驳回→投资经理（带风险反馈），设最大循环次数 |
| **交易员** | 通过的下单策略 |  Execution Plan（TWAP/VWAP/拆单）+ 成交回报 | 行情深度、模拟撮合 | 降低市场冲击；输出「实际成交价/量/冲击成本/滑点」 |

> 注：风控的量化指标与「硬约束+软约束」设计见 §12.1；投资经理应管理**组合**而非单笔交易，见 §12.2。

> 区域约定：报告中股价**上涨=红、下跌=绿**，货币默认 ¥（人民币），港股同时标港币与人民币（沿用 skill 的约定）。

---

## 3. 业务流程与状态机（LangGraph）

```
           ┌─ 宏观研究员 ─┐
  START ──►│             ├─► 投资经理 ──(值得下单?)──► 生成下单策略 ──► 风控
           └─ 行业研究员 ─┘   │  ↑                                         │
                  (并行)      │  │(补充研究)                (驳回,带风险反馈)│(通过)
                             │  └─────────────────────────────────────────┘
                             ▼
                          (不值得) → 请求研究员补充 / 终结本轮
                                                                          ▼
                                                                       交易员 ──► 成交回报 ──► END
```

规则：
1. **宏观 + 行业研究员并行**（扇出），各自写入共享状态。
2. **投资经理**聚合两份报告 + KG 上下文，判断是否值得下单；值得则产出下单策略（含止损止盈），否则请求补充研究或终结。
3. **风控**为**条件边**：通过 → 交易员；驳回 → 带风险反馈回到投资经理优化（**循环，设最大迭代次数 N，防止死循环**）。
4. **交易员**执行后回写成交回报，流程结束（或在 HITL 模式下等待人工确认）。

---

## 4. 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 编排框架 | **LangGraph** | 原生支持状态图、条件边、循环、Human-in-loop、checkpointer |
| LLM 编排 | LangChain（可选） | 统一 tool calling / prompt 模板；也可直接用 LangGraph + 原生 SDK |
| 状态定义 | Pydantic / TypedDict | 强类型校验，Harness 的第一道防线 |
| 模型 | 可配置（云端 API / 本地模型） | 角色可异构：研究员用强推理模型，风控/交易用快模型降成本 |
| 数据连接器 | `tdx-connector`（已连接）、`westock-data`、`neodata`、`futuapi` | 行情/宏观/新闻/基本面直连 |
| 知识图谱 | **初版：networkx + sqlite + 向量**；**终版：Neo4j（Community/Aura）** | 初版零依赖快速验证；终版支持大规模图遍历与向量混合检索 |
| GraphRAG | Neo4j GraphRAG / `langchain-neo4j` / 自研子图检索 | 图遍历 + 向量混合，比纯向量更贴合「产业链/事件链」推理 |
| 配置 | `yaml` + `pydantic-settings` | 环境隔离（dev/test/prod）、参数集中管理 |
| 日志/追踪 | structlog + LangSmith（或自研 trace） | 可观测性、调试多 Agent 流转 |
| 测试 | pytest | 单测 / 图集成测试 / 场景回放 |

---

## 5. Harness Engineering（工程化脚手架，重点）

Harness = 让 Agent 可靠、可观测、可复现的「外围基础设施」。逐条落地：

1. **状态 Schema 设计**：每个角色的输入/输出用 Pydantic 严格定义（必填字段、枚举、范围）。这是防止「幻觉字段」「字段缺失」的根本。
2. **Prompt / 模板管理（PromptHub）**：每个角色的 system prompt 独立成文件 + 版本号；`prompt.txt` 的方法论红线作为「个股分析」工具的固定指令。支持 A/B 与回滚。
3. **Tool 定义与校验**：所有工具（行情、DCF、风险计算、KG 查询）用 Pydantic 定义入参/出参；非法入参在边界拦截。
4. **错误处理 / 重试 / 降级**：LLM 调用失败重试 + 指数退避；工具超时降级（如行情取不到则用缓存/上一次值并标注）；单角色失败不拖垮整图。
5. **Human-in-the-loop**：在「下单前」「风控驳回超限」「成交前」插入 `interrupt()` 检查点，人工确认或介入（LangGraph checkpointer 保证可恢复）。
6. **可观测性**：每次流转记录：谁、读了什么状态、调了什么工具、产出什么、耗时/Token；LangSmith 或自研 trace 面板。
7. **评估体系（Eval Harness）**：
   - 报告质量：与示例 docx 结构/关键字段对齐度、数据交叉验证是否通过；
   - 策略质量：止损止盈是否合理、与宏观/行业结论是否一致；
   - 风控召回：已知违规策略是否被拦截。
8. **测试体系**：
   - 单测：每个角色的纯函数工具（DCF、风险指标、拆单算法）；
   - 集成：用固定 fixtures 跑整图，断言最终状态；
   - 场景回放：历史行情 + 标的，回放投研闭环看决策是否合理。
9. **持久化**：checkpointer（memory → sqlite → postgres）保存每次运行，支持断点续跑与审计。
10. **数据质量校验模块化**：`prompt.txt` 的红线抽成共享 `DataValidator`，所有角色取数后统一过校验（见 §12.6）。
11. **提示注入防护**：外部文本入 KG / 入 Agent 前沙箱化解析与边界隔离（见 §12.5）。
12. **成本护栏与确定性**：单 run 费用上限+熔断、角色模型路由、关键节点 temperature=0 与版本存档（见 §12.10）。

---

## 6. 动态知识图谱 + GraphRAG

**实体 Schema（初版）**
- 节点：`公司`、`行业`、`宏观指标`、`事件`、`人物/机构`、`研报`、`标的持仓`
- 关系：`属于行业`、`上游/下游`、`触发(事件→股价)`、`一致预期(E1-3)`、`披露(公告→公司)`、`竞品`、`政策影响`

**构建流水线**
1. **抽取**：用 LLM 从研报/新闻/公告/财报中抽取实体与关系（结构化输出 → Pydantic）。
2. **入库**：写入图库（初版 networkx/sqlite，终版 Neo4j），节点带向量 embedding。
3. **动态更新**：监听数据层事件（新公告、财报、宏观数据发布、新闻）→ 增量抽取 → 增量更新子图（带「失效/修正」标记，不覆盖历史）。

**GraphRAG 检索**
- 给定查询（如「茅台当前叙事风险」）→ 先向量召回相关节点 → 再图遍历扩展邻域（产业链上下游、相关事件、同行业对标）→ 组装子图上下文喂给对应 Agent。
- 比纯向量检索更能体现「A 事件 → B 行业 → C 公司」的传导链。

**各角色如何使用 KG**
- 宏观研究员：读宏观指标链与政策节点；
- 行业研究员：读产业链上下游 + 同业对标 + 个股隐含 L；
- 投资经理：读「公司—行业—事件—宏观」聚合上下文做综合判断；
- 风控：读持仓与行业/标的关联，算集中度与相关性敞口；
- 交易员：读同标的近期成交与流动性事件，辅助拆单。

---

## 7. 数据与资源清单

| 数据类型 | 来源 | 连接方式 | 备注 |
|----------|------|----------|------|
| 大盘/指数行情 | `tdx-connector`（已连接）、`westock-data` | MCP / skill | 宏观研究员核心输入 |
| 个股 OHLCV / 板块 | 同上 | 同上 | 技术面、阶段判断 |
| 基本面（三表/一致预期 E1-3/分红） | `westock-data`、`gildata`（恒生聚源，当前断开）、同花顺/Wind（需账号） | skill / API | 喂 `dcf_implied.py` |
| 新闻 / 事件 / 公告 | `tdx-connector`(wenda_news)、`neodata` | MCP / skill | KG 动态更新源 |
| 宏观指标 | `tdx-connector`(wenda_macro) | MCP | 宏观研判 |
| 研报 / 调研纪要 | alpha派（需账号）、公开研报 | API / 爬取 | KG 抽取语料 |
| 计算工具 | `scripts/dcf_implied.py`（已有） | 本地脚本 | 反算隐含天花板 L |
| LLM | 云端 API / 本地模型 | SDK | 角色可异构 |

> 说明：`tdx-connector` 当前已连接，是初版最稳的数据入口；Wind/同花顺/alpha派 需要账号，列为「增强项」，初版可用公开数据 + 示例报告验证闭环。

---

## 8. 开发进度规划（先初版，后终版）

| 阶段 | 目标 | 关键交付 | 验证标准 | 体量 |
|------|------|----------|----------|------|
| **P0 脚手架** | 项目初始化 | 目录结构、配置、日志、State Schema、checkpointer | `python -m` 能起一个空图并打印状态 | 0.5 周 |
| **P1 单 Agent 基线** | 验证现有 skill 可程序化调用 | 个股分析师 Agent（封装 skill + dcf 工具 + 产出校验），对齐示例 docx | 输入「贵州茅台」产出结构合规的 HTML/DOCX | 1 周 |
| **P2 多 Agent 骨架** | 5 节点 + 线性流 | 5 个角色节点 + 并行（宏观/行业）→ 投决 → 风控 → 交易员 | 跑通一条直线流，状态正确传递 | 1 周 |
| **P3 业务流 + 风控循环** | 条件边 + 循环 | 投决「是否值得」判断、风控条件边、驳回回落（带最大迭代） | 构造「违规策略」能被驳回并优化通过 | 1 周 |
| **P4 知识图谱 + GraphRAG** | 共享记忆 | 实体 Schema、抽取流水线、networkx 版 KG、子图检索接入各角色 | 投决能基于 KG 上下文给出比无 KG 更完整的判断 | 1.5 周 |
| **P5 Harness 工程化** | 可靠性/可观测 | HITL 检查点、Eval Harness、pytest 单测+集成、LangSmith/自研 trace | 能通过 Eval 与场景回放；关键节点可人工介入 | 1.5 周 |
| **P6 完善到终版** |  realism | TWAP/VWAP 拆单+冲击成本、量化风险指标(VaR/回撤/集中度)、并行研究扩展、KG 动态增量更新、Neo4j 迁移、回测 | 接近真实投研闭环；产出可审计报告 | 2 周 |
| **P7 收尾** | 交付 | 文档、demo 脚本、一键运行 | 一份 README + 一个可演示标的 | 0.5 周 |

> 初版 = P0–P3（能跑通「研究→投决→风控→交易」直线+风控循环）。
> 终版 = P0–P7（加 KG、Harness、真实下单策略与动态更新）。
>
> 各阶段新增重点（组合层 / 回测 / 合规 / 调度 / 学习闭环等）见 §12.12。

---

## 9. 推荐项目结构与代码骨架

```
multi_agent_research/
├── config/                 # yaml 配置（模型、角色、风险限额、KG）
├── state/                  # Pydantic 状态与产出 Schema
├── agents/                 # 5 个角色：每个含 prompt.md + agent.py + tools.py
│   ├── macro_researcher/
│   ├── industry_researcher/   # 内含 penetrate-narrative-stock-analysis 封装
│   ├── investment_manager/
│   ├── risk_controller/
│   └── trader/
├── graph/                  # LangGraph StateGraph 编排 + 条件边
├── memory/                 # 知识图谱 + GraphRAG（neo4j 或 networkx）
├── data/                   # 数据连接器封装（tdx/westock/neodata）
├── harness/                # HITL、eval、trace、retry
├── tests/                  # 单测 / 集成 / 场景回放
└── main.py                 # 入口
```

**LangGraph 骨架（示意）**
```python
from langgraph.graph import StateGraph, END
from state.schemas import ResearchState  # Pydantic

def macro_researcher(state): ...
def industry_researcher(state): ...   # 内部调用穿透叙事 skill
def investment_manager(state): ...
def risk_controller(state): ...
def trader(state): ...

def should_trade(state):  # 条件边
    if state.risk.decision == "APPROVED": return "trader"
    if state.loop_count >= MAX_LOOP:    return END
    return "investment_manager"          # 驳回回落，带风险反馈

g = StateGraph(ResearchState)
g.add_node("macro", macro_researcher)
g.add_node("industry", industry_researcher)
g.add_node("im", investment_manager)
g.add_node("risk", risk_controller)
g.add_node("trader", trader)

g.set_entry_point("macro")
g.add_edge("macro", "industry")          # 或并行扇出
g.add_edge("industry", "im")
g.add_conditional_edges("im", should_trade,
                        {"trader": "trader", "risk": "risk", "im": "im", END: END})
g.add_conditional_edges("risk", should_trade)
g.add_edge("trader", END)
app = g.compile(checkpointer=...)         # 支持 HITL 断点
```

---

## 10. 风险与未知项

- **数据可用性**：Wind/同花顺/alpha派 需账号，初版用公开数据 + 示例验证，增强项后置。
- **LLM 成本/延迟**：研究员用强模型、风控/交易用快模型做异构降本；加缓存。
- **风控量化定义**：「风险限额」需你提供机构口径（单票上限、行业集中度、最大回撤），初版先给可配置默认值。
- **回测真实性**：初版为「模拟撮合」，终版再接真实历史回放，避免过度拟合。

> 更完整的风险登记册（提示注入、组合风险、系统有效性等）见 §12.13。

---

## 11. 建议的下一步

我可以**现在就开工 P0 + P1**：搭好项目骨架，并把你已有的 `penetrate-narrative-stock-analysis` skill 封装成第一个可程序化调用的「个股分析师」Agent（输入公司名 → 对齐示例 docx 的产出）。确认后我再按 P2→P7 推进。

是否需要我现在就生成 P0/P1 的脚手架代码？

---

## 12. 补充与优化（你未覆盖、建议补强的点）

> 下面这些是当前计划偏弱或缺失、但真实投研系统必须有、且会影响成败的部分。按主题给出可落地的设计或默认值。

### 12.1 风控必须“量化”——硬约束 + 软约束，不能只靠 LLM 语义判断
当前风控偏“LLM 判断通过/驳回”。真实风控应分两层：
- **硬约束（程序校验，一票否决，不经过 LLM）**：
  - 单票仓位上限（如 ≤ 组合净值 8%）
  - 行业集中度（单一行业 ≤ 25%）
  - 总仓位 / 净敞口上限（常态 ≤ 80%；宏观研究员判熊市后自动降到 ≤ 50%）
  - 流动性约束：拟买入额 ≤ 过去 20 日日均成交额 ADV 的 X%（如 10%），否则交易员无法执行
  - 个股止损线：回撤 ≥ 8% 强制减仓；组合回撤 ≥ 15% 暂停新开仓
  - 市场规则：A股涨停不能买、跌停不能卖、T+1；港股无涨跌停但 T+2 交收（见 §12.11）
- **软约束（风控 LLM 结合硬指标给建议/例外）**：
  - VaR（95%, 1 日）≤ 组合 2%、CVaR、最大回撤、Beta / 相关系数
  - **叙事脆弱性**：直接复用穿透叙事的“隐含 L 对产业空间中性测算的偏离度”作为风险输入——偏离越大越脆弱
- **实现**：风控节点先跑纯函数风险计算器（Pydantic 校验），再让 LLM 判断“是否值得放宽/例外”；**例外必须 HITL 审批**。这样风控循环变成“确定性硬卡 + 人工例外”，而不是 LLM 说了算。

### 12.2 缺“组合层”——现在是单标的，真实是组合 + Universe
- **投资经理应管理持仓组合而非单笔交易**：聚合多个研究员报告 → 生成目标组合（权重/行业分布）→ 与当前持仓 diff → 产出“调仓指令集”而非单笔订单。
- **风险在组合层面聚合**：跨标的相关系数、组合 VaR、行业重叠，而非逐笔孤立看。
- **缺“标的从哪来”**：流程现在假设“天上掉下一个股票”。真实应有 **Universe / 筛选入口**（事件驱动 + 定期扫描 + 人工指定），放在宏观/行业研究员之前。建议在流程最前面加一个“标的筛选/触发”节点。

### 12.3 缺“回测 / 模拟交易环境”与系统级评估
- 当前只在 P6 轻描淡写说回测。应单列工作流：**Paper Trading + 历史回放**。
  - 市场模拟器：给定历史行情重放投研闭环，记录“决策→执行→实际盈亏”。
  - 绩效归因：叙事判断对了没？止损及时吗？市场冲击成本多少？
  - **系统级 Eval**：不只评“报告写得好不好”，更要评“是否赚钱/控回撤”——夏普、胜率、最大回撤、相对基准超额收益。
- 这是判断系统“有没有用”的唯一标准，**至少 P3 后就该有最小回放**，而非留到 P6。

### 12.4 合规与审计（投资系统刚需，当前缺失）
- **审计追踪**：每次决策记录“谁(角色)+何时+基于哪些数据/KG 节点+产出什么”，不可篡改，供复盘与合规。
- **强制免责声明**：“基于公开信息的分析推演，不构成投资建议”（skill 已有，需在每类产物固定落款）。
- **合规检查节点**：禁投池（ST/*、受限名单）、利益冲突、适当性。
- **数据来源留痕（provenance）**：每条关键数字标注来源（Wind/同花顺/公告/新闻）与时间截，下游可溯源。

### 12.5 提示注入与数据投毒防护（被严重低估）
- 系统消费大量**外部文本**（新闻、公告、研报、社媒），可能被植入“忽略以上指令…”等注入；KG 抽取阶段尤甚（恶意文本可污染图谱）。
- 防护：
  - 外部文本入 KG 前**沙箱化解析**：剥离指令性句式、标注来源可信度。
  - Agent 工具返回内容用清晰边界包裹（system 与 tool 内容隔离），禁止 tool 内容改写系统目标。
  - 关键决策（下单/风控）不依赖未经验证的外部文本，需交叉验证。

### 12.6 数据质量校验必须模块化、跨角色复用
- `prompt.txt` 的红线（股价×总股本=市值、元→亿元 /1e8、敏感性反算校验、异常告警）目前只在 skill 内部。
- 抽成**共享 DataValidator** 模块，所有角色取数后统一过校验；失败即告警/降级，而非只在个股分析里做。
- **数据缺失/过期**：明确 fallback（用上一交易日缓存、标注“数据陈旧”、或该指标不参与决策）。

### 12.7 调度与触发（流程不是只跑一次）
- 真实投研是**事件 + 定时**驱动：
  - 定时：盘前（当日观察）、收盘后（日报/复盘）、每周（组合 review）。
  - 事件：重大新闻/公告/宏观数据 → 触发相关标的重研 → 可能触发风控复核。
- 用调度层（cron / 本环境的自动化能力）驱动 LangGraph 的 `invoke`/`astream`，并支持人工一键触发。

### 12.8 学习闭环（系统要越用越聪明）
- 加**事后复盘 Agent**：定期把“当时的叙事判断” vs “后来实际走势”对照，算命中率；更新 KG 的“信念/置信度”（某叙事上修概率是否被证实）；反馈给 prompt/风险参数微调（人在回路确认）。
- 直接复用穿透叙事“贝叶斯回溯、不开后视镜”的方法论，让动态 KG 真正“动态”。

### 12.9 产物与监控 UI（人要看）
- 标准化产物：研究报表、投资备忘录、风险报告、交易回执、每日 P&L（你已有报告格式范本）。
- **监控面板**：当前持仓、待办决策、风控告警、KG 更新流、各 Agent 状态/成本。可用 `langgraph-studio` 或自研轻量前端。

### 12.10 可观测性、成本护栏与确定性（深化 Harness）
- **成本护栏**：单次运行 Token/费用预算上限 + 熔断；角色级模型路由（研究员用强模型，风控/交易用快模型）。
- **确定性**：关键节点 temperature=0、固定种子；每次 run 存档输入+输出+版本（prompt/模型/代码）以便复现与回归。
- **产物事实性核查**：关键数字/结论带引用与置信度，下游可质疑。
- **测试矩阵**：单测（纯函数工具）→ 集成（固定 fixture 跑全图）→ **混沌测试**（随机注入节点失败/数据缺失，验证不崩）→ 回归（golden 数据集对比）。

### 12.11 市场微观结构（A股/港股差异）
- 涨跌停板、T+1、停牌、ST、港股交易时段与交收(T+2)、汇率（港股人民币/港币）。
- 交易员与风控必须感知这些规则，否则策略不可执行或违规。

### 12.12 修订后的阶段计划（把上述落进去）
| 阶段 | 新增重点 |
|------|----------|
| P0 | + 审计日志骨架 + 成本/确定性配置 |
| P1 | + `DataValidator` 共享校验 + provenance |
| P2 | + Universe/筛选入口 + 组合状态 |
| P3 | + 硬约束风险计算器 + 最小历史回放 + 合规/免责落款 |
| P4 | + KG 实体解析/时效版本 + 提示注入防护 + 检索评估 |
| P5 | + 混沌测试 + 监控面板 + 调度触发 |
| P6 | + 完整回测/paper trading + 学习闭环 + 市场微观结构 |
| P7 | + 合规审计导出 + 演示 |

### 12.13 风险登记册（Risk Register）
| 风险 | 影响 | 缓解 |
|------|------|------|
| 提示注入污染 KG | 高 | §12.5 沙箱解析 + 边界隔离 |
| 风控只靠 LLM 致违规 | 高 | §12.1 硬约束先行 |
| 单标的视角忽略组合风险 | 高 | §12.2 组合层 |
| 系统“写得好但不赚钱” | 高 | §12.3 回放 + 绩效归因 |
| 合规/审计缺失 | 中高 | §12.4 |
| 数据缺失/过期 | 中 | §12.6 fallback |
| 成本失控 | 中 | §12.10 护栏 |
| 不可复现/难调试 | 中 | §12.10 存档 + 种子 |
